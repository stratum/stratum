# Copyright 2022-present Intel
# SPDX-License-Identifier: Apache-2.0

version: 2.1
setup: true
orbs:
  continuation: circleci/continuation@0.1.2
jobs:
  setup: 
    docker:
      - image: golang:1.13-alpine
    steps:
      - checkout
      - run:
          name: Install git
          command: apk add git
      - run:
          name: Generate config
          command: |
            # Check if the Dockerfile.build is modified in this revision
            git diff main HEAD --stat | grep Dockerfile.build &> /dev/null
            [[ $? == 0 ]] && export NEW_BUILDER_FLAG="--new-builder"
            cd .circleci && go run generate-config.go $NEW_BUILDER_FLAG > /tmp/generated_config.yml
      - continuation/continue:
          configuration_path: /tmp/generated_config.yml # use newly generated config to continue

# our single workflow, that triggers the setup job defined above
workflows:
  setup:
    jobs:
      - setup